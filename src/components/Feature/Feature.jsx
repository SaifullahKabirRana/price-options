import React from 'react';
import PropTypes from 'prop-types';

const Feature = props => {
    return (
        <div>
            
        </div>
    );
};

Feature.propTypes = {
    
};

export default Feature;